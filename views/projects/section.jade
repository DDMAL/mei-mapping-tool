h3 Create Section 

form(action='/section',method='post',enctype='application/x-www-form-urlencoded' id = "section1")  
   h5 Name : 
   input#inputName(class = "createProjectInput" type="text", placeholder="ex. Section-1", name = "nameSection" required)
   
   p Select a first neume
    select(style = "display : block" name="firstNeume" id = "selectFirstNeume" required)
     option(disabled='', selected='', value='')  -- select a first neume --
      - each neume in neumes 
         option(value='#{neume.id}') #{neume.name} 
   
   p Select a second neume
    select(style = "display : block" name="secondNeume" id = "selectUser" required)
     option(disabled='', selected='', value='')  -- select a second neume --
       - each neume in neumes 
         option(value='#{neume.id}') #{neume.name} 
 
   button( id ="submitSection" class = "button3CancelImage" onclick = "myFunction()") Submit

